spring.application.name=ChemLearn
jwt.secret=${JWT_SECRET}

spring.datasource.url=jdbc:postgresql://localhost:5432/chemlearn
spring.datasource.username=admin
spring.datasource.password=Nguyenanphu2005@
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.sql.init.mode=never

#logging.level.org.hibernate.engine.internal=DEBUG
#spring.web.error.include-message=always

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

app.frontend.allowed-origins=${APP_FRONTEND_ALLOWED_ORIGINS:http://localhost:5173,http://103.153.254.119,https://chemlearn.com.vn}

app.seed-admin.enabled=${SEED_ADMIN_ENABLED:true}
app.seed-admin.username=${SEED_ADMIN_USERNAME:duckhisuu}
app.seed-admin.password=${SEED_ADMIN_PASSWORD:Hieubeep1407@}
app.seed-admin.email=${SEED_ADMIN_EMAIL:duckhisuu@chemlearn.local}
app.seed-admin.full-name=${SEED_ADMIN_FULL_NAME:Duck Hisuu}
app.seed-admin.sync-password=${SEED_ADMIN_SYNC_PASSWORD:true}
app.required-seed.enabled=${REQUIRED_SEED_ENABLED:true}
app.required-seed.replace-premade-labs=${REQUIRED_SEED_REPLACE_PREMADE_LABS:true}

ai.provider=${AI_PROVIDER:mock}
ai.cache-enabled=${AI_CACHE_ENABLED:true}
ai.fallback-enabled=${AI_FALLBACK_ENABLED:true}
ai.model=${AI_MODEL:${GEMINI_MODEL:mock}}
ai.api-key=${AI_API_KEY:${GEMINI_API_KEY:${GOOGLE_API_KEY:}}}
ai.temperature=${AI_TEMPERATURE:0.4}
ai.request-timeout-seconds=${AI_REQUEST_TIMEOUT_SECONDS:60}
ai.app-title=${AI_APP_TITLE:ChemLearn}
ai.app-url=${AI_APP_URL:http://localhost:5173}
ai.openrouter.base-url=${OPENROUTER_BASE_URL:https://openrouter.ai/api/v1/chat/completions}
ai.gemini.base-url=${GEMINI_BASE_URL:https://generativelanguage.googleapis.com/v1beta/models}

spring.servlet.multipart.max-file-size=8MB
spring.servlet.multipart.max-request-size=9MB

spring.mail.host=${BREVO_SMTP_HOST:smtp-relay.brevo.com}
spring.mail.port=${BREVO_SMTP_PORT:587}
app.frontend.base-url=${APP_FRONTEND_BASE_URL:http://localhost:5173}
app.mail.from=${APP_MAIL_FROM:support@chemlearn.com.vn}
app.mail.from-name=${APP_MAIL_FROM_NAME:ChemLearn}
app.mail.reply-to=${APP_MAIL_REPLY_TO:chemlearnvn@gmail.com}
app.mail.mascot-url=${APP_MAIL_MASCOT_URL:}
app.mail.header-url=${APP_MAIL_HEADER_URL:}
app.mail.notifications.enabled=${APP_MAIL_NOTIFICATIONS_ENABLED:true}
app.mail.automatic.enabled=${APP_MAIL_AUTOMATIC_ENABLED:true}
app.mail.scheduler-zone=${APP_MAIL_SCHEDULER_ZONE:Asia/Ho_Chi_Minh}
app.mail.daily-reminder.enabled=${APP_MAIL_DAILY_REMINDER_ENABLED:true}
app.mail.daily-reminder.cron=${APP_MAIL_DAILY_REMINDER_CRON:0 0 7,20 * * *}
app.companion-reminder.enabled=${APP_COMPANION_REMINDER_ENABLED:true}
app.mail.fallback.enabled=${APP_MAIL_FALLBACK_ENABLED:true}
app.mail.fallback.host=${GMAIL_SMTP_HOST:smtp.gmail.com}
app.mail.fallback.port=${GMAIL_SMTP_PORT:587}
app.mail.fallback.username=${GMAIL_SMTP_USERNAME:${GMAIL:}}
app.mail.fallback.password=${GMAIL_SMTP_PASSWORD:}
spring.mail.username=${BREVO_SMTP_USERNAME:af691b001@smtp-brevo.com}
spring.mail.password=${BREVO_SMTP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# cong thanh toan
payos.client-id=${PAYOS_CLIENT_ID}
payos.api-key=${PAYOS_API_KEY}
payos.checksum-key=${PAYOS_CHECKSUM_KEY}
